---
import { Icon } from 'astro-icon/components';
import { getHomePermalink } from '~/utils/permalinks';
import LogoWithIcon from '~/components/common/LogoWithIcon.astro';

interface Link {
  text?: string;
  href?: string;
  ariaLabel?: string;
  icon?: string;
}

interface Links {
  title?: string;
  links: Array<Link>;
}

interface EcoDesignBadge {
  text: string;
  icon: string;
  details: string;
}

export interface Props {
  links: Array<Links>;
  socialLinks: Array<Link>;
  legalLinks: Array<Link>;
  footNote: string;
  ecoDesignBadge: EcoDesignBadge;
}

const { socialLinks = [], links = [], legalLinks = [], footNote, ecoDesignBadge } = Astro.props;
---

<footer class="relative not-prose bg-gradient-to-b from-white to-[--ootb-turquoise]">
  <div class="mx-auto max-w-7xl px-4 sm:px-6">
    <!-- Section principale -->
    <div class="py-16">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 mb-16">
        <!-- Logo et Description -->
        <div class="lg:col-span-5 space-y-8 text-center lg:text-left">
          <div class="flex justify-center lg:justify-start">
            <a href={getHomePermalink()} class="inline-block transition-transform hover:scale-105 duration-300">
              <LogoWithIcon size="lg" />
            </a>
          </div>
          <div class="text-gray-800 space-y-4 hidden lg:block">
            <p class="max-w-md leading-relaxed">
              Out of the Books connecte et inspire les acteurs du changement éducatif en Francophonie. Notre festival et Nos contenus créent des espaces d'échange pour réinventer l'éducation.
            </p>
          </div>
        </div>

        <!-- Navigation 3 colonnes -->
        <div class="lg:col-span-7 grid grid-cols-1 md:grid-cols-3 gap-8 mb-12">
          {
            [links[2], links[1], links[0]].map(({ title, links }) => (
              <div class="space-y-4 text-center lg:text-left">
                <h3 class="text-gray-900 font-semibold text-lg tracking-wide">{title}</h3>
                {links && Array.isArray(links) && links.length > 0 && (
                  <ul class="space-y-3">
                    {links.map(({ text, href, ariaLabel }) => (
                      <li>
                        <a
                          class="text-gray-800 hover:text-white transition-colors duration-300"
                          href={href}
                          aria-label={ariaLabel}
                        >
                          {text}
                        </a>
                      </li>
                    ))}
                  </ul>
                )}
              </div>
            ))
          }
        </div>
      </div>

      <!-- Badge Écoconception -->
      <div class="flex justify-center mb-4">
        <a 
          href="https://www.websitecarbon.com/" 
          target="_blank" 
          rel="noopener noreferrer" 
          class="group flex items-center gap-2 py-2.5 px-3.5 bg-white/40 backdrop-blur-sm rounded-lg border border-white/30 hover:bg-white/50 transition-all duration-300"
        >
          <Icon 
            name={ecoDesignBadge.icon} 
            class="w-3.5 h-3.5 text-gray-600/80" 
          />
          <div class="flex flex-col">
            <div class="text-sm font-medium text-gray-800/90 mb-1">Site écoconçu et optimisé</div>
            <div class="flex items-center gap-1 text-xs text-gray-600/80 pb-2">
              Plus écologique que 90% des sites web testés
              <span class="ml-1 text-[10px] text-gray-500/70 group-hover:text-gray-600/80 transition-colors duration-300">
                • En savoir plus
                <Icon name="tabler:arrow-right" class="inline w-2.5 h-2.5 -mt-[1px] ml-0.5 group-hover:translate-x-0.5 transition-transform duration-300" />
              </span>
            </div>
          </div>
        </a>
      </div>

      <!-- Séparateur -->
      <div class="h-px bg-gradient-to-r from-transparent via-gray-400/30 to-transparent mb-8"></div>

      <!-- Footer bottom -->
      <div class="flex flex-col items-center md:flex-row md:justify-between gap-6">
        <!-- Liens légaux -->
        <div class="flex gap-6 text-sm text-gray-800 md:w-1/3 md:justify-start">
          {
            legalLinks.map(({ text, href }) => (
              <a href={href} class="hover:text-white transition-colors duration-300">
                {text}
              </a>
            ))
          }
        </div>

        <!-- Réseaux sociaux -->
        <div class="flex gap-5 md:w-1/3 justify-center">
          {
            socialLinks.map(({ ariaLabel, href, icon }) => (
              <a
                class="w-10 h-10 flex items-center justify-center rounded-full bg-white/95 border border-gray-200 text-gray-800 hover:bg-white/80 hover:text-[--ootb-turquoise] hover:border-white hover:scale-110 transition-all duration-300"
                href={href}
                target="_blank"
                rel="noopener noreferrer"
                aria-label={ariaLabel}
              >
                {icon && <Icon name={icon} class="w-5 h-5" />}
              </a>
            ))
          }
        </div>

        <!-- Copyright et crédit développeur -->
        <div class="text-sm text-gray-800 md:w-1/3 md:text-right">
          <span set:html={footNote} />
          <span class="mx-1.5 text-gray-400">•</span>
          <a 
            href="https://pwablo.be" 
            target="_blank" 
            rel="noopener noreferrer"
            class="text-gray-600 hover:text-white transition-colors duration-300"
          >
            Design & code par Pwablo
          </a>
        </div>
      </div>
    </div>
  </div>
</footer>

<style>
/* Animation subtile pour les liens */
a:not([class*="rounded-full"]) {
  position: relative;
  padding-bottom: 2px;
}

a:not([class*="rounded-full"])::after {
  content: '';
  position: absolute;
  width: 0;
  height: 1px;
  bottom: 0;
  left: 0;
  background: linear-gradient(90deg, white, white);
  opacity: 0.7;
  transition: width 0.3s ease;
}

a:not([class*="rounded-full"]):hover::after {
  width: 100%;
}

/* Amélioration de la lisibilité sur mobile */
@media (max-width: 768px) {
  .text-gray-800 {
    --tw-text-opacity: 1;
  }
  
  .bg-white\/95 {
    --tw-bg-opacity: 0.98;
  }
}
</style>