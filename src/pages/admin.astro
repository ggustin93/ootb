---
// Configuration Tina CMS pour Astro
---

<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Administration Out of the Books</title>
    <script 
      src="https://unpkg.com/@tinacms/cli@latest/dist/tina.js" 
      crossorigin
    ></script>
  </head>
  <body class="tinacms">
    <div id="root"></div>
    <script>
      const init = async () => {
        try {
          // 🚀 Configuration Tina Cloud
          await window.tina.init({
            clientId: import.meta.env.TINA_CLIENT_ID,
            branch: 'main',
            tokenStorage: 'LOCAL_STORAGE',
            cmsCallback: (cms) => {
              // Activation des fonctionnalités admin
              cms.flags.set('tina-admin', true);
            },
            // Configuration de l'API
            apiURL: import.meta.env.DEV 
              ? 'http://localhost:4001/graphql'
              : `https://content.tinajs.io/content/${import.meta.env.TINA_CLIENT_ID}/github/main`
          });

          console.log('✅ Tina CMS initialisé avec succès');
        } catch (error) {
          console.error('❌ Échec de l\'initialisation de Tina', error);
        }
      };

      init();
    </script>
  </body>
</html>
