---
import Layout from '~/layouts/PageLayout.astro';
import { Icon } from 'astro-icon/components';
import { getHomePermalink } from '~/utils/permalinks';
import Button from '~/components/ui/Button.astro';
import '~/assets/styles/handwritten.css';

const title = `Merci pour votre contribution`;
const description = `Votre fiche pédagogique a été soumise avec succès. Nous vous remercions pour votre contribution à l'innovation pédagogique.`;
---

<Layout metadata={{ title, description }}>
  <section class="flex items-center min-h-[70vh] p-8 md:p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-2xl text-center">
        <div class="mb-12 flex justify-center">
          <div class="bg-[--ootb-turquoise] text-white p-6 rounded-full shadow-lg">
            <Icon name="tabler:check" class="w-16 h-16" />
          </div>
        </div>
        
        <h1 class="text-4xl md:text-5xl font-handwritten mb-8 text-[--ootb-blue] leading-tight">
          Merci pour votre contribution !
        </h1>
        
        <p class="text-xl md:text-2xl mb-8 text-gray-700 leading-relaxed max-w-xl mx-auto">
          Votre fiche pédagogique a été soumise avec succès. Notre équipe l'examinera prochainement.
        </p>
        
        <p class="mb-12 text-lg text-gray-600 leading-relaxed max-w-lg mx-auto">
          Nous vous remercions de partager votre expertise et de contribuer à l'innovation pédagogique.
        </p>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6 max-w-2xl mx-auto">
          <Button 
            variant="blue" 
            size="lg" 
            href={getHomePermalink()} 
            icon="tabler:home" 
            iconPosition="left"
            class="min-h-[60px]"
          >
            Retour à l'accueil
          </Button>
          
          <Button 
            variant="outline-blue" 
            size="lg" 
            href="/category/fiche" 
            icon="tabler:book" 
            iconPosition="left"
            class="min-h-[60px]"
          >
            Découvrir les fiches
          </Button>
        </div>
      </div>
    </div>
  </section>
</Layout> 